{
  "name": "trpc",
  "version": "0.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "prebuild": "yarn generate && yarn migrate",
    "dev": "dotenv -e .env.local -- nodemon --files src/server.ts",
    "build": "yarn prebuild && tsc",
    "start": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "prisma generate",
    "db-seed": "prisma db seed",
    "db-reset": "prisma migrate dev reset",
    "migrate-dev": "prisma migrate dev",
    "migrate": "prisma migrate deploy",
    "prisma-studio": "prisma studio"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "dotenv-cli": "^7.3.0",
    "eslint-config-custom": "*",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "tsconfig": "*",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@fastify/cors": "8.4.1",
    "@prisma/client": "5.5.2",
    "@trpc/server": "^10.43.3",
    "fastify": "^4.24.3",
    "prisma": "5.5.2",
    "trpc-panel": "^1.3.4",
    "zod": "^3.22.4"
  }
}
